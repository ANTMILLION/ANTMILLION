<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.antmillion.stock.mapper.StockOrderMapper">

    <insert id="insertOrder" useGeneratedKeys="true" keyProperty="orderId">
        INSERT INTO stock_order (
            account_id, 
            stock_code, 
            transaction_type, 
            order_type, 
            quantity, 
            order_price, 
            status, 
            created_at, 
            updated_at
        ) 
        VALUES (
            #{accountId}, 
            #{stockCode}, 
            #{transactionType}, 
            #{orderType}, 
            #{quantity}, 
            #{orderPrice}, 
            #{status}, 
            #{createdAt}, 
            #{updatedAt}
        )
    </insert>

    <select id="selectWaitOrders" resultType="com.antmillion.stock.dto.StockOrderResponseDTO">
        SELECT 
            o.*, 
            s.stock_name,
            COALESCE(SUM(t.trade_quantity), 0) as executedQuantity,
            (o.quantity - COALESCE(SUM(t.trade_quantity), 0)) as remainedQuantity
        FROM stock_order o
        JOIN stock s ON o.stock_code = s.stock_code
        LEFT JOIN trade_log t ON o.order_id = t.order_id
        WHERE o.account_id = #{accountId} 
          AND o.status IN ('WAIT', 'PARTIAL')
        GROUP BY o.order_id
        ORDER BY o.created_at DESC
    </select>

    <update id="cancelOrder">
        UPDATE stock_order 
        SET status = 'CANCEL', 
            updated_at = NOW()
        WHERE order_id = #{orderId} 
          AND account_id = #{accountId}
          AND status IN ('WAIT', 'PARTIAL')
    </update>

    <update id="updateOrder">
        UPDATE stock_order
        SET order_price = #{orderPrice},
            quantity = #{quantity},
            updated_at = NOW()
        WHERE order_id = #{orderId}
          AND status = 'WAIT'
          AND account_id = #{accountId}
    </update>

    <select id="getOrderByOrderId" resultType="com.antmillion.stock.dto.StockOrderResponseDTO">
        SELECT * FROM stock_order WHERE order_id = #{orderId}
    </select>

    <select id="findExecutableOrders" resultType="com.antmillion.stock.dto.StockOrderResponseDTO">
        SELECT 
            o.*, 
            s.stock_name, 
            COALESCE(SUM(t.trade_quantity), 0) as executedQuantity
        FROM stock_order o
        JOIN stock s ON o.stock_code = s.stock_code
        LEFT JOIN trade_log t ON o.order_id = t.order_id
        WHERE o.stock_code = #{stockCode} 
          AND o.status IN ('WAIT', 'PARTIAL')
          AND (
               (o.transaction_type = 'BUY' AND o.order_price &gt;= #{currentPrice})
               OR 
               (o.transaction_type = 'SELL' AND o.order_price &lt;= #{currentPrice})
          )
        GROUP BY o.order_id 
        ORDER BY o.created_at ASC
    </select>

    <update id="updateOrderStatus">
        UPDATE stock_order 
        SET status = #{status}, 
            updated_at = NOW() 
        WHERE order_id = #{orderId}
    </update>

    <update id="cancelRemainingOrders">
        UPDATE stock_order 
        SET status = 'CANCEL' 
        WHERE status IN ('WAIT', 'PARTIAL')
    </update>

    <select id="getSumUnexecutedQty" resultType="java.lang.Integer">
        SELECT SUM(o.quantity - COALESCE(t.exec_sum, 0))
        FROM stock_order o
        LEFT JOIN (
            SELECT order_id, SUM(trade_quantity) as exec_sum
            FROM trade_log 
            GROUP BY order_id
        ) t ON o.order_id = t.order_id
        WHERE o.account_id = #{accountId}
          AND o.stock_code = #{stockCode}
          AND o.transaction_type = #{type}
          AND o.status IN ('WAIT', 'PARTIAL')
    </select>

</mapper>
