<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.antmillion.stock.mapper.InterestMapper">

    <!-- 관심종목 추가 -->
    <insert id="insertInterest" parameterType="com.antmillion.stock.dto.InterestDTO">
        INSERT INTO interest (stock_code, account_id, created_at)
        VALUES (#{stockCode}, #{accountId}, NOW())
    </insert>

    <!-- 관심종목 삭제 -->
    <delete id="deleteInterest">
        DELETE FROM interest
        WHERE stock_code = #{stockCode}
          AND account_id = #{accountId}
    </delete>

    <!-- 특정 계정의 관심종목 코드 목록 조회 -->
    <select id="selectInterestStockCodesByAccountId" resultType="String">
        SELECT stock_code
        FROM interest
        WHERE account_id = #{accountId}
        ORDER BY created_at DESC
    </select>

    <!-- 특정 종목이 관심종목인지 확인 -->
    <select id="countInterest" resultType="int">
        SELECT COUNT(*)
        FROM interest
        WHERE stock_code = #{stockCode}
          AND account_id = #{accountId}
    </select>

</mapper>