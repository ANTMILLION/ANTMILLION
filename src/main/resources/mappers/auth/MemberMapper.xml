<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.antmillion.auth.mapper.MemberMapper">

  <resultMap id="MemberMap" type="com.antmillion.user.dto.MemberDTO">
    <id property="userId" column="user_id"/>
    <result property="rankId" column="rank_id"/>
    <result property="email" column="email"/>
    <result property="password" column="password"/>
    <result property="nickname" column="nickname"/>
    <result property="point" column="point"/>
    <result property="provider" column="provider"/>
  </resultMap>

  <insert id="insertMember" parameterType="com.antmillion.user.dto.MemberDTO"
          useGeneratedKeys="true" keyProperty="userId">
    INSERT INTO member (rank_id, email, password, nickname, point, provider)
    VALUES (#{rankId}, #{email}, #{password}, #{nickname}, #{point}, #{provider})
  </insert>

  <select id="countByEmail" resultType="int">
    SELECT COUNT(*) FROM member WHERE email = #{email}
  </select>

  <select id="countByNickname" resultType="int">
    SELECT COUNT(*) FROM member WHERE nickname = #{nickname}
  </select>

  <select id="selectByEmail" resultMap="MemberMap">
    SELECT user_id, rank_id, email, password, nickname, point, provider
    FROM member
    WHERE email = #{email}
  </select>

  <update id="updateUserPoint">
    UPDATE member
    SET point = LEAST(10000, GREATEST(0, COALESCE(point, 0) + #{point}))
    WHERE user_id = #{userId}
  </update>

  <update id="updateUserRank">
    UPDATE member
    SET rank_id = #{rankId}
    WHERE user_id = #{userId}
  </update>

  <select id="selectUserPoint" resultType="int">
    SELECT point
    FROM member
    WHERE user_id = #{userId}
  </select>

  <select id="selectUserNickName" resultType="String">
    SELECT nickname
    FROM member
    where user_id = #{userId}
  </select>
</mapper>